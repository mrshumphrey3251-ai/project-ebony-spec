# Project Ebony: Kinetic Guillotine Veto Specifications
**System Architecture:** Evont Bare-Metal Core
**Status:** Public Specification / Redacted Blueprint
**Classification:** Cloud-Deficient Deterministic Control Loop

## 1. Operational Overview
The Kinetic Guillotine Veto serves as the absolute hardware-level boundary for the Evont system. It operates entirely within local execution space on the NVIDIA Jetson Orin platform, bypassing all high-level software agent logic and probabilistic reasoning loops. Its singular mission is to map raw physical distance directly to hardware relay state via Pulse Width Modulation (PWM) overrides.

## 2. Spatial Veto Matrix Core Logic
The following reference logic represents the foundational binary safety threshold. It ensures that regardless of the intentional commands generated by autonomous scheduling or operator input, physical proximity acts as an un-bypassable hard veto.

```python
# Project Ebony: Standard Guillotine Veto (Redacted)
# Public blueprint for basic binary spatial hazard evaluation

def evaluate_spatial_veto(intended_pwm, spatial_distance_mm):
    # Standard static perimeter testing value
    STANDARD_CRITICAL_ZONE_MM = 300.0
    
    if intended_pwm <= 0.0:
        return intended_pwm

    if spatial_distance_mm <= STANDARD_CRITICAL_ZONE_MM:
        print("[Veto Engine] Hazard detected. Severing relays.")
        return 0.0
    else:
        return intended_pwm
